part of tke_item_store;

class ${fileBasenameNoExtension} extends F_UI.StatelessWidget {
  ${fileBasenameNoExtension}({F_UI.Key? key}) : super(key: key);

  F_UI.Widget build(F_UI.BuildContext context) {
    return Box(
      width: AxisSize.shrinkToFitContents(),
      height: AxisSize.tu(7),
      children: [],
    );
  }
}
