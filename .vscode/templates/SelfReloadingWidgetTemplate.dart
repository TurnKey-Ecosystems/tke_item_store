part of tke_item_store;

class ${fileBasenameNoExtension} extends SelfReloadingWidget {
  ${fileBasenameNoExtension}({F_UI.Key? key})
    : super(
      key: key,
      reloadTriggers: [],
    );

  F_UI.Widget buildWidget(F_UI.BuildContext context) {
    return Box(
      width: AxisSize.shrinkToFitContents(),
      height: AxisSize.tu(7),
      children: [],
    );
  }
}
